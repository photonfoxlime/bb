[package]
name = "bb"
version = "0.1.0"
edition = "2024"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
[dependencies]
dioxus = { version = "0.7.3", features = ["desktop", "logger"] }
uuid = { version = "1.20.0", features = ["v4"] }
